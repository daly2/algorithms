
void recursion(int n)
{

  for(int i=0;i < 100;i++)
  {
    int x = sin(0.8+0.234);
  }

  if (n > 0) {
    recursion(n/2);
    recursion(n/2);
  }
}

when timed this will follow the geometric series f(n)=(2^(log_2(n))-1), a binary tree.
n^2=O(2^(n+1)-1)

omega(f)=n

theta(f)=n